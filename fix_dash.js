const fs = require('fs'); let code = fs.readFileSync('public/js/dashboard.js', 'utf8'); const eventDelegation = \ndocument.addEventListener(" click\, function(e) {\n const btn = e.target.closest(\.btn-sim\);\n if (btn) {\n const giftName = btn.getAttribute(\data-gift\);\n const coins = parseInt(btn.getAttribute(\data-coins\), 10);\n const count = parseInt(btn.getAttribute(\data-count\), 10);\n socket.emit(\simulateGift\, { giftName: giftName, coins: coins, count: count, nickname: \Donador de Prueba\ });\n }\n});\n; code = code.replace(/document\.querySelectorAll\(\x27\.btn-sim\x27\)[\s\S]*/, eventDelegation); fs.writeFileSync('public/js/dashboard.js', code); console.log('Dashboard actualizado');