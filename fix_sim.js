const fs = require('fs'); let code = fs.readFileSync('server.js', 'utf8'); code = code.replace('uniqueId: data.username || 'DonanteSimulado',', 'uniqueId: data.username || 'DonanteSimulado', nickname: data.nickname || 'Donante de Prueba','); code = code.replace(/currentCoins \+= coinValue;/g, 'currentCoins += coinValue;'); fs.writeFileSync('server.js', code); console.log('Servidor revisado');